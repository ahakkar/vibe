name: Run Python Tests

on:
  push:
    branches:
      - main
      - unitTestingInitialize
      - robotTesting
  pull_request:
    branches:
      - main
      - unitTestingInitialize
      - robotTesting

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'  # Change to the version you need

      - name: Install Dependencies
        run: |
          sudo apt update
          sudo apt install portaudio19-dev
          
      - name: Install pip Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r tests/requirements.txt  # Install dependencies

      - name: Run Tests
        run: |
          pytest tests
